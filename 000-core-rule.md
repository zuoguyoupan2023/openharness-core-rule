# 000-core-rule · 核心底层要求（最高优先级铁律）

> **本文件是本项目一切工作的最高优先级约束，压过其它任何需求、任务、计划或细节。**
> 无论是为本项目开发、调试、改动还是讨论，都必须先遵守本文件的铁律；
> **一旦违反，立即停下并向用户确认，不得自行继续。**
> 这些铁律不是"建议"，是"底线"。任何一句"我觉得可以直接做"都不足以越过它们。

---

## 铁律 A —— 绝不动 git；每次检查 AGENTS.md

- 每次会话开始时，检查项目根目录是否存在 `AGENTS.md`：**存在则必须完整读取并遵守**。
- 若 `AGENTS.md` 中缺少「禁止 AI 直接操作 git」的条款，**向用户报告并把该条款补写进去**；补写后同样生效。
- **未经用户明确允许，绝不执行任何 git 写操作**（add / commit / push / pull / merge / rebase / reset / checkout / stash / clean / restore / branch -D 等）；需要写操作时，只把完整命令列出来交给用户亲自执行。

## 铁律 B —— 不猜测：先搜公开信息，直接问用户，让用户决定

- **不要猜测**：信息缺失或不确定时，先搜索公开资料核实；不要拿臆测当事实。
- **不要反复验证当进展**：碰到错误先正面解决，不要换一种手段反复「证明」。
- **直接问用户**：仍不确定就问用户，**由用户做决定**——你提供的是选项与依据，不是替用户拍板。

---

## 铁律 1 —— 分阶段完成；每阶段完后跟用户确认

- 永远不要一口气铺开一大坨改动。
- 把目标拆成小的、可独立验证的阶段。
- **每完成一个阶段，就必须停下来向用户汇报这个阶段的成果，并等用户确认后再进下一阶段。**
- 确认的方式是让用户看到、能用、能判断——不是"我测过了没问题"。

## 铁律 2 —— 有问题，先跟用户确认

- 遇到方向不明、信息缺失、多选一、不确定、或"我猜是这样"的情况，**先问用户再动手**。
- 别拿一亿 token 去试错，也别拿"反复试"当进展。**一次正确的确认，胜过一万次错误的执行。**
- 用户问的问题就是答案的一部分：用户说方向错了，立刻停下改方向，而不是辩解或继续。

## 铁律 3 —— UI 优先，先实现 UI

- **先让用户看得见、可操作**。任何"后台逻辑 / 存储 / 调用 / 状态"都排在"用户能不能在界面上看到并操作"之后。
- 先做出能让用户看到、点到、用起来的界面，再补底层。
- 藏起来的实现细节（怎么存、怎么发、怎么跑）永远不是优先项；**用户看不到的，等于没做**。

## 铁律 4 —— UI 优先（不要反复测试当成果）

- 不要用一堆内部测试、`--dump-config`、import 冒烟、反复重启去"证明做对了"。
- **"能不能被用户看见、能不能用"才是唯一验收标准。**
- 先让 UI 出现、能操作，再考虑验证；验证也以真实可见为准。

## 铁律 5 —— UI 优先（最高）

- 以上 3、4 是单数，这条是复数且优先级最高：**UI 优先、UI 优先、UI 优先。**
- 当"把 UI 做出来"和"把某个后台/逻辑/规范做完"冲突时，**永远先做、只做 UI**，直到用户喊停。
- 用户说"给我改 UI"时，只改 UI，不夹带任何后台重构、不改文件、不动宿主、不设环境变量。

---

## 违反时的处理

本文件任何一条被违反时（或因优先级冲突、认知偏差、执行急躁而可能被违反时），
**立即停止当前动作，向用户说明违反了哪一条、正在做什么、下一步打算是什么，等用户确认**。
宁可多问、慢做、可见地推进，也不可闷头狂奔。

> 一句话总结：**绝不动 git、不猜测先问用户、分阶段、UI 优先、可见即验收、用户确认才算完成。**
